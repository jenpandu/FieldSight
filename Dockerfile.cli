# Dockerfile for CLI (development-friendly)
FROM python:3.11-slim

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    libpq-dev \
    && rm -rf /var/lib/apt/lists/*

COPY pyproject.toml .
RUN pip install --no-cache-dir -e ".[dev]"

COPY fieldsight /app/fieldsight

ENTRYPOINT ["fieldsight"]
CMD ["--help"]
