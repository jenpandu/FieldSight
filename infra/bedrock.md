# Bedrock

- Model access approved for: reasoning, fast, embedding, multimodal, judge (request early).
- Record model ids, versions, provisioned throughput/quotas per model.
- Guardrail: content filters + Prompt Attacks filter; record id and version.
- Knowledge Base: OpenSearch Serverless collection, embedding model, data source with chunking NONE, metadata fields doc_type / section_path.
